import React from 'react';

const MainPage = () => {
    const onClickSignout = async () => {};

    return (
        <div>
            <button
                onClick={onClickSignout}
            >
                Sign out
            </button>
        </div>
    );
};

export default MainPage;
